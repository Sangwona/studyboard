{
  "build": {
    "cmd": "npm --prefix frontend install && npm --prefix frontend run build && mv frontend/dist backend/static && pip install -r backend/requirements.txt"
  },
  "start": {
    "cmd": "python backend/app.py"
  }
}
